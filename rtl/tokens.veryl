interface TokensParserIf {
    var placeholder: bool;
    modport self {
        placeholder: output,
    }

    modport user {
        placeholder: input,
    }
}

module TokensParser (
    clk: input clock,
    rst: input reset,

    bd: modport BoolDecoderIf::user,

    self: modport TokensParserIf::self,
) {
    // Parse the bit stream
}
