interface SyntaxParserIf {
    modport self {


    }

    modport user {


    }
}

module SyntaxParser (
    clk: input clock,
    rst: input reset,

    bd: modport BoolDecoderIf::user,

    self: modport SyntaxParserIf::self,
) {
    // Parse macro blocks somehow
    // actually i think i need two parsers one for the frame header
    // and others for the mb headers and i dont know about the
    // tokens since they come on another partition
}
